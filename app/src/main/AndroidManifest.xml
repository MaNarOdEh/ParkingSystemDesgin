<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pccorner.parkingsystemdesgin">

    <application
        android:allowBackup="true"
        android:icon="@drawable/car"
        android:label="@string/app_name"
        android:roundIcon="@drawable/car"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LogInActivity" />
        <activity android:name=".FirstScreen" />
        <activity android:name=".AdminMainActivity" />
        <activity android:name=".profile_admin" />
        <activity android:name=".Profile_cars_owner" />
        <activity android:name=".search_by_viedo" />
        <activity android:name=".add_new_car" />
        <activity android:name=".car" />
        <activity android:name=".Employee" />
        <activity android:name=".Add_Employee"></activity>
    </application>

</manifest>